# src/parked — PARKED code (PLAN §15)

Kept for a possible future paid tier per PLAN §15; **not routed** and never
imported from app code. Files here must still typecheck, but nothing under
`src/app` may import them while the tier is parked.
